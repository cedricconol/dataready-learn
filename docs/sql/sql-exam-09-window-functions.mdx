---
id: sql-exam-09-window-functions
title: "Exam 9: Window Functions"
sidebar_label: "Exam 9: Window Functions"
sidebar_position: 108
youtube_url: ""
---

import SqlExercise from '@site/src/components/SqlExercise';

# Exam 9: Window Functions

**Difficulty:** Advanced

**Topics covered:** `OVER()`, `PARTITION BY`, `ROW_NUMBER`, `RANK`, `LAG`, running totals with `SUM() OVER`

Each question shows the target output. Write a SQL query that produces exactly those columns and rows.

---

## Question 1

Rank all orders by `total_amount` (highest first) using `RANK()`. Return `order_id`, `total_amount`, and `rank`.

<SqlExercise
  tables={["orders"]}
  expected={{
    columns: ["order_id", "total_amount", "rank"],
    rows: [
      [1, 999, 1],
      [7, 999, 1],
      [9, 999, 1],
      [4, 796, 4],
      [3, 349, 5],
      [6, 349, 5],
      [8, 89.97, 7],
      [2, 59.98, 8],
      [5, 49.9, 9],
      [10, 24.95, 10],
    ]
  }}
/>

---

## Question 2

Rank each customer's orders by `total_amount` descending using `RANK() OVER (PARTITION BY customer_id ...)`. Return `customer_id`, `order_id`, `total_amount`, and `rank_within_customer`.

<SqlExercise
  tables={["orders"]}
  expected={{
    columns: ["customer_id", "order_id", "total_amount", "rank_within_customer"],
    rows: [
      [1, 1, 999, 1],
      [1, 6, 349, 2],
      [1, 2, 59.98, 3],
      [2, 7, 999, 1],
      [2, 3, 349, 2],
      [3, 9, 999, 1],
      [3, 4, 796, 2],
      [4, 5, 49.9, 1],
      [4, 10, 24.95, 2],
      [5, 8, 89.97, 1],
    ]
  }}
/>

---

## Question 3

Calculate a running total of `total_amount` across all orders ordered by `order_date`. Return `order_id`, `order_date`, `total_amount`, and `running_total`.

<SqlExercise
  tables={["orders"]}
  expected={{
    columns: ["order_id", "order_date", "total_amount", "running_total"],
    rows: [
      [1, "2024-01-05", 999, 999],
      [2, "2024-01-10", 59.98, 1058.98],
      [3, "2024-01-15", 349, 1407.98],
      [4, "2024-02-01", 796, 2203.98],
      [5, "2024-02-10", 49.9, 2253.88],
      [6, "2024-03-01", 349, 2602.88],
      [7, "2024-03-15", 999, 3601.88],
      [8, "2024-04-01", 89.97, 3691.85],
      [9, "2024-04-10", 999, 4690.85],
      [10, "2024-05-01", 24.95, 4715.8],
    ]
  }}
/>

---

## Question 4

For each order (ordered by `order_date`), show the previous order's `total_amount` using `LAG` aliased as `prev_order_amount`, and the difference from the current order as `amount_change`. Return `order_id`, `order_date`, `total_amount`, `prev_order_amount`, and `amount_change`.

<SqlExercise
  tables={["orders"]}
  expected={{
    columns: ["order_id", "order_date", "total_amount", "prev_order_amount", "amount_change"],
    rows: [
      [1, "2024-01-05", 999, null, null],
      [2, "2024-01-10", 59.98, 999, -939.02],
      [3, "2024-01-15", 349, 59.98, 289.02],
      [4, "2024-02-01", 796, 349, 447],
      [5, "2024-02-10", 49.9, 796, -746.1],
      [6, "2024-03-01", 349, 49.9, 299.1],
      [7, "2024-03-15", 999, 349, 650],
      [8, "2024-04-01", 89.97, 999, -909.03],
      [9, "2024-04-10", 999, 89.97, 909.03],
      [10, "2024-05-01", 24.95, 999, -974.05],
    ]
  }}
/>

---

## Question 5

Using `ROW_NUMBER()` partitioned by `customer_id` and ordered by `order_date` descending, find each customer's most recent order (row number = 1). Return `customer_id`, `order_id`, `order_date`, `total_amount`, and `status`.

<SqlExercise
  tables={["orders"]}
  expected={{
    columns: ["customer_id", "order_id", "order_date", "total_amount", "status"],
    rows: [
      [1, 6, "2024-03-01", 349, "cancelled"],
      [2, 7, "2024-03-15", 999, "completed"],
      [3, 9, "2024-04-10", 999, "pending"],
      [4, 10, "2024-05-01", 24.95, "pending"],
      [5, 8, "2024-04-01", 89.97, "completed"],
    ]
  }}
/>
